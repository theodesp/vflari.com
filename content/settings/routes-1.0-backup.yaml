routes:

collections:
  /:
    permalink: '{globals.permalinks}' # special 1.0 compatibility setting. See the docs for details.
    template:
      - index
  /my-poems/:
    permalink: /my-poems/{slug}/

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
